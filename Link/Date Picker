#Selenium4 Date Picker
from selenium import webdriver
from selenium.webdriver import Keys
from selenium.webdriver.common.by import By
from selenium.webdriver.chrome.service import Service
from webdriver_manager.chrome import ChromeDriverManager
from selenium.webdriver.support.select import Select
import time
#Future Date
# driver = webdriver.Chrome(service=Service(ChromeDriverManager().install()))
# driver.get("https://jqueryui.com/datepicker/")
# driver.maximize_window()
# driver.maximize_window()
# time.sleep(2) #lowers the performance
#
# driver.switch_to.frame(0) #month date year format - mm/dd/yyyy
# # driver.find_element(By.XPATH, "//*[@id='datepicker']").send_keys("05/03/2022")
#
# year="2025"
# month="October"
# day="30"
# driver.find_element(By.ID, "datepicker").click()
# time.sleep(2)
# while True:
#      mon=driver.find_element(By.XPATH, "//span[@class='ui-datepicker-month']").text
#      yr = driver.find_element(By.XPATH, "//span[@class='ui-datepicker-year']").text
#      if mon==month and yr==year:
#          break
#      else:
#        driver.find_element(By.XPATH,"//*[@id='ui-datepicker-div']/div/a[2]/span").click()
#
# #select date now
# All_dates=driver.find_elements(By.XPATH,"//div[@id='ui-datepicker-div']//table/tbody/tr/td/a")
# for unique_date in All_dates:
#     if unique_date.text==day:
#         unique_date.click()
#         break
#Past Date
driver = webdriver.Chrome(service=Service(ChromeDriverManager().install()))
driver.get("https://www.dummyticket.com/dummy-ticket-for-visa-application/")
driver.maximize_window()
driver.maximize_window()
driver.maximize_window()
time.sleep(2) #lowers the performance

#DOB
driver.find_element(By.XPATH,"//*[@id='dob']").click()
datepicker_month=Select(driver.find_element(By.XPATH,"//*[@id='ui-datepicker-div']/div[1]/div/select[1]"))
datepicker_month.select_by_visible_text("Dec")

datepicker_year=Select(driver.find_element(By.XPATH,"//*[@id='ui-datepicker-div']/div[1]/div/select[2]"))
datepicker_year.select_by_visible_text("1990")
date_req="3"
datepicker_dates=driver.find_elements(By.XPATH,"//*[@id='ui-datepicker-div']/table/tbody/tr/td/a")
for unique_dates in datepicker_dates:
    if unique_dates.text == date_req:
        unique_dates.click()
        break

input("Press Enter to close browser...")





