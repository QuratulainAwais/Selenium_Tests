#Selenium4 XPath Axes
from selenium import webdriver
from selenium.webdriver.common.by import By
from selenium.webdriver.chrome.service import Service
from webdriver_manager.chrome import ChromeDriverManager
import time

driver = webdriver.Chrome(service=Service(ChromeDriverManager().install()))

#driver.get("https://demo.nopcommerce.com/register")
#driver.get("https://opensource-demo.orangehrmlive.com/")
# driver.maximize_window()
# time.sleep(2)

#Application Commands
# print(driver.title)
# print(driver.current_url)
# print(driver.page_source)

#Conditional Commands
# search= driver.find_element(By.XPATH,"//input[@id='small-searchterms']")
# print(search.is_displayed()) #validation
# print(search.is_enabled()) #validation
#
# #is_selected() radio buttons or check boxes
# check_selected=driver.find_element(By.ID,"gender-male")
# print(check_selected.is_selected()) #False because it is not selected
# check_selected.click() # After clicking radio button it turns to True
# print(check_selected.is_selected())



#Browser Commands
#close, quit()
# driver.find_element(By.LINK_TEXT, "OrangeHRM, Inc").click()
# #driver.close() #Only first page close that is homepage / parent browser window
# driver.quit() #Entire browser closes including child browser window / internally terminating the driver's processes
#

#Navigational Commands / back() forward () refresh()
# driver.get("https://www.snapdeal.com")
# driver.get("https://www.amazon.com")
# driver.back()
# driver.forward()
# driver.refresh()



#find_element() - return single element only
# element_driver=driver.find_element(By.XPATH,"//input[@id='small-searchterms']")
# element_driver.send_keys("Apple Macbook Pro 13-inch")

#locator matching with multiple elements
# element_driver=driver.find_element(By.XPATH,"//div[@class='footer']//a")
# print(element_driver.text) #Prints first link from footer only

#if no element is found using "element" then NoSuchElementException is thrown but this is not the case when "elements" is used
# login_element= driver.find_element(By.LINK_TEXT,"Log")
# login_element.click()

#Locator matching with multiple webelements
# elements= driver.find_elements(By.XPATH,"//div[@class='footer']//a")
# print(len(elements))
# for ele in elements:
#     print(ele.text)


#clear text present already
# driver.get("https://admin-demo.nopcommerce.com/login")
# driver.maximize_window()
# time.sleep(2)
# emailbox=driver.find_element(By.XPATH, "//input[@id='Email']")
# emailbox.clear()
# emailbox.send_keys("abc@gmail.com")
# print(emailbox.text) #Returns inner text of the element
# print(emailbox.get_attribute("value")) #displays what value is present

#where text works
driver.get("https://admin-demo.nopcommerce.com/login")
driver.maximize_window()
time.sleep(2)
emailbox=driver.find_element(By.XPATH, "//button[normalize-space()='Log in']")
print(emailbox.text)
print(emailbox.get_attribute('value'))
print(emailbox.get_attribute('type'))


input("Press Enter to close browser...")
